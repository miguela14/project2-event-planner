<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
        rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM"
        crossorigin="anonymous">
    <title>Event Horizon</title>
</head>

<body>

    <!-- navbar -->

    <nav class="navbar navbar-expand-lg bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand text-white" href="./"><img class="img-fluid p-2" style="max-width: 50px; max-height: 50px;"
                    src="https://cdn-icons-png.flaticon.com/512/2738/2738419.png"><strong>EventHorizon</strong></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active text-white" aria-current="page" href="/">Home</a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown"
                            aria-expanded="false">
                            Account
                        </a>
                        <ul class="dropdown-menu">
                            {{#if logged_in}}
                            <li><a class="dropdown-item" href="/profile">Profile</a></li>
                            {{else}}
                            <li><a class="dropdown-item" href="/login">Login</a></li>
                            {{/if}}
                        </ul>
                    </li>
                    {{#if logged_in}}
                    <li><button class="btn btn-toolbar text-white" id="logout">Logout</button></li>
                    {{/if}}
                </ul>
            </div>
        </div>
    </nav>
    <!-- navbar -->
    <div class="">
        {{{ body }}}
    </div>

    <!-- footer  -->
    <section class="text-center bg-primary text-white py-3">
        <div class="container text-center text-md-start mt-5">
            <div class="row mt-3">
                <div class="col-md-3 col-lg-4 col-xl-3 mx-auto mb-4">
                    <h1>EventHorizon</h1>
                </div>
                <div class="col-md-4 col-lg-3 col-xl-3 mx-auto mb-md-0 mb-4">
                    <h6 class="text-uppercase fw-bold mb-4">Contact</h6>
                    <p><i class=""></i>Minnesota, US</p>
                    <p>
                        <i class=""></i>
                        eventhorizon@gmail.com
                    </p>
                    <p><i class=""></i> +1 202-555-0191</p>
                    <p><i class=""></i> +1 415-555-7832</p>
                    <p><i class=""></i> +1 678-555-3210</p>
                </div>
            </div>
        </div>
    </section>
    <div class="text-center p-4 bg-secondary text-white">
        © 2023 Copyright
    </div>
    </div>
    </div>
    </footer>
    <!-- footer  -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz"
        crossorigin="anonymous"></script>
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>